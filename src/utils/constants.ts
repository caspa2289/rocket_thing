export enum GAME_STAGES {
    beforeStarted = 'beforeStarted',
    inProgress = 'inProgress',
    finished = 'finished',
}

export const API_BASE_PATH = 'https://rocketbackend.up.railway.app/api/v1'
export const USER_API_PATH = `${API_BASE_PATH}/user`
